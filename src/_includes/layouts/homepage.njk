{% extends "base.njk" %}

{% block navbar %}

    <nav class="navbar navbar-expand-lg site-navigation">
      <div class="container">
        <a class="navbar-brand" href="/">
          <img src="{{navLogo}}" alt="nav-logo" />
        </a>
        <a class="nav-link btn btn-sm btn-primary btn-sm-rounded" href="/{{navBtnLink}}">
          <span class="btn-area">
            <span data-text="{{navBtnText}}">
              {{navBtnText}}
            </span>
          </span>
        </a>
      </div>
    </nav>

{% endblock %}

{% block content %}

    <section class="site-hero">
      <div class="container">
        <div class="row">
          <div class="col-lg-10 mx-auto">
            <div class="site-hero-content text-center">
              <h6>{{hero.subTitle}}</h6>
              <h1>
                {{hero.title.firstPara}}
                <br>
                {{hero.title.secondPara}}
              </h1>
              <ul class="site-hero-content-buttons">
                <li>
                  <a href="{{hero.seeWorkBtn.link}}" class="btn btn-secondary scroll-to">
                    <span class="btn-area">
                      <span data-text="{{hero.seeWorkBtn.text}}">
                        {{hero.seeWorkBtn.text}}
                      </span>
                    </span>
                  </a>
                </li>
                <li>
                  <a href="{{hero.connectBtn.link}}" class="btn btn-primary scroll-to">
                    <span class="btn-area">
                      <span data-text="{{hero.connectBtn.text}}">
                        {{hero.connectBtn.text}}
                      </span>
                    </span>
                  </a>
                </li>
              </ul>
            </div>
          </div>
        </div>
      </div>
      {# <a href="#counter" class="site-hero-scroll scroll-to">
        <img src="/assets/images/arrow-down.svg" alt="arrow-down">
      </a> #}
    </section>

    {# <section class="site-counter" id="counter">
      <div class="container">
        <div class="row">
          <div class="col-12">
            <div class="section-title">
              <h2 class="text-white">{{achieve.title}}</h2>
            </div>
          </div>
          {% for achievement in achieve.achievements %}
          <div class="col-lg-3 col-md-6">
            <div class="site-counter-item">
              <span class="site-counter-item-title">{{achievement.title}}</span>
              <h3 class="site-counter-item-number">{{achievement.counter}}+</h3>
            </div>
          </div> 
          {% endfor %}
        </div>
      </div>
    </section> #}

    {# <section class="site-project" id="project">
      <div class="container">
        <div class="row">
          <div class="col-12">
            <div class="section-title">
              <h2>{{portfolio.subTitle}}</h2>
              <p>{{portfolio.title}}</p>
            </div>
          </div>
          {% for portfolio in collections.portfolios %}
          <div class="col-lg-6 col-md-10 mx-auto">
            <div class="site-project-item">
              <div class="site-project-item-thumb">
                <img src="{{portfolio.data.img}}" alt="project-thumb">
              </div>
              <div class="site-project-item-content">
                <span>{{portfolio.data.subTitle}}</span>
                <h3>{{portfolio.data.title}}</h3>
                <a href="{{portfolio.data.page.url}}" class="read-more">{{portfolio.data.linkText}}</a>
              </div>
            </div>
          </div>
          {% endfor %}
          <div class="col-12 text-center text-lg-left">
            <a href="/{{portfolio.seeAllbtn.link}}" class="site-project-cta">{{portfolio.seeAllbtn.text}}</a>
          </div>
        </div>
      </div>
    </section> #}

    {# agrego para test el blog detail aca #}

  <section class="site-blog details">
      <div class="container">
        <div class="row justify-content-center">
          <div class="col-lg-8">
            <article class="site-blog-details">
              {# <p><span>{{blogpost.data.postDate}}</span> by <span>{{blogpost.data.author}}</span></p> #}
              <h2 class="blog-title">{{introduction.title}}</h2>
              {# <img class="feature-image" src="{{blogpost.data.featureImg}}" alt="blog-details-image"> #}
              {{content | safe}}
            </article>
          </div>
        </div>
      </div>
    </section>

    <section class="site-testimonial" id="servicios">
      <div class="container">
        <div class="row">
          {# <div class="col-12">
            <div class="section-title">
              <h2>
                {{testimonials.title}}
              </h2>
              <p>
                {{testimonials.des}} 
              </p>
            </div>
          </div> #}
          {# {% for opinion in {{services}} %} #}
          <div class="col-lg-4 col-md-6">
            <div class="site-testimonial-item">
              <div class="site-testimonial-item-header">
                {# <div class="thumb">
                  <img src="{{opinion.img}}" alt="user-thumb">
                </div> #}
                <div class="person">
                  <h5>{{services.import.title}}</h5>
                  {# <p>{{opinion.dig}}</p> #}
                </div>
              </div>
              <p class="site-testimonial-item-body">
                {{services.import.description}}
              </p>
            </div>
          </div> 

          <div class="col-lg-4 col-md-6">
            <div class="site-testimonial-item">
              <div class="site-testimonial-item-header">
                {# <div class="thumb">
                  <img src="{{opinion.img}}" alt="user-thumb">
                </div> #}
                <div class="person">
                  <h5>{{services.export.title}}</h5>
                  {# <p>{{opinion.dig}}</p> #}
                </div>
              </div>
              <p class="site-testimonial-item-body">
                {{services.export.description}}
              </p>
            </div>
          </div> 

          <div class="col-lg-4 col-md-6">
            <div class="site-testimonial-item">
              <div class="site-testimonial-item-header">
                {# <div class="thumb">
                  <img src="{{opinion.img}}" alt="user-thumb">
                </div> #}
                <div class="person">
                  <h5>{{services.intervention.title}}</h5>
                  {# <p>{{opinion.dig}}</p> #}
                </div>
              </div>
              <p class="site-testimonial-item-body">
                {{services.intervention.description}}
              </p>
            </div>
          </div> 

          <div class="col-lg-4 col-md-6">
            <div class="site-testimonial-item">
              <div class="site-testimonial-item-header">
                {# <div class="thumb">
                  <img src="{{opinion.img}}" alt="user-thumb">
                </div> #}
                <div class="person">
                  <h5>{{services.consultancy.title}}</h5>
                  {# <p>{{opinion.dig}}</p> #}
                </div>
              </div>
              <p class="site-testimonial-item-body">
                {{services.consultancy.description}}
              </p>
            </div>
          </div> 

          <div class="col-lg-4 col-md-6">
            <div class="site-testimonial-item">
              <div class="site-testimonial-item-header">
                {# <div class="thumb">
                  <img src="{{opinion.img}}" alt="user-thumb">
                </div> #}
                <div class="person">
                  <h5>{{services.logistics.title}}</h5>
                  {# <p>{{opinion.dig}}</p> #}
                </div>
              </div>
              <p class="site-testimonial-item-body">
                {{services.logistics.description}}
              </p>
            </div>
          </div> 

          <div class="col-lg-4 col-md-6">
            <div class="site-testimonial-item">
              <div class="site-testimonial-item-header">
                {# <div class="thumb">
                  <img src="{{opinion.img}}" alt="user-thumb">
                </div> #}
                <div class="person">
                  <h5>{{services.internationalMoving.title}}</h5>
                  {# <p>{{opinion.dig}}</p> #}
                </div>
              </div>
              <p class="site-testimonial-item-body">
                {{services.internationalMoving.description}}
              </p>
            </div>
          </div> 

          {# {% endfor %} #}
        </div>
      </div>
    </section>

    <footer class="site-footer" id="contacto">
      <div class="container">
        <div class="row">
          <div class="col-lg-4 col-md-6">
            <div class="site-footer-widget">
              <h5 class="site-footer-widget-title">{{footer.contactHeading}}</h5>
              <p class="site-footer-widget-description">
                <a href="{{footer.email.link}}">{{footer.email.text}}</a>
                <br>
                <a href="{{footer.location.link}}">{{footer.location.text}}</a>
                <br>
              {# {% for contactInfo in footer.contactsInfo %}
                <a href="{{contactInfo.link}}">{{contactInfo.text}}</a>
                <br>
              {% endfor %} #}
              </p>
            </div>
          </div>
          <div class="col-lg-5 col-md-6">
            <iframe src="https://www.google.com/maps/embed?pb=!1m14!1m8!1m3!1d13134.789453368472!2d-58.3737494!3d-34.6118135!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x0%3A0xe6f9c76d2f124393!2sEMB%20Consultores%20SRL!5e0!3m2!1sen!2sar!4v1669690888837!5m2!1sen!2sar" width="350" height="225" style="border:0;" allowfullscreen="" loading="lazy" referrerpolicy="no-referrer-when-downgrade"></iframe>
          </div>
          <div class="col-lg-3 col-md-6">
            <a href="#top" class="site-footer-widget-top">
              <img src="{{footer.back2topIcon}}" alt="back-to-top">
              <p>
                {{footer.back2topIconText}}
              </p>
            </a>
          </div>
          <div class="col-12">
            <div class="site-footer-copyright text-center my-3">
              <p>© Copyright 2022 - All Rights Reserved by EMB Consultores</p>
            </div>
          </div>
        </div>
      </div>
    </footer>
  
{% endblock %}